apiVersion: v1
kind: Service
metadata:
  name: ping-ms
  namespace: ping
  labels:
    app: ping
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: ping
    tier: backend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-ms
  namespace: ping
  labels:
    app: ping
    tier: backend
spec:
  selector:
    matchLabels:      
      app: ping
      tier: backend
  replicas: 2
  template:
    metadata:
      name: ping-ms
      labels:
        app: ping
        tier: backend
    spec:
      containers:
        - name: ping-ms
          image: kipu23/ping-ms:1.0.0
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: PingDatabaseSettings__ConnectionString
              value: mongodb://ping:pong@ping-mongo:27017/ping
            - name: PingDatabaseSettings__DatabaseName
              value: ping
            - name: PingDatabaseSettings__CollectionName
              value: pings